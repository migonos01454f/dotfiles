# Autostart programs
## Gnome Keyring
exec-once = dbus-update-activation-environment --all
exec-once = gnome-keyring-daemon --start --components=secrets
## Network manager applet
exec-once = nm-applet --indicator
## Polkit Agent
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
## Microphone muter
exec-once = mute-microphone
## Battery notifier
exec-once = battery-level-notifier
## Wallpaper
exec-once = ~/.azotebg-hyprland
## Status Bar:
exec-once = waybar -b bar0

# Env vars.
env = XCURSOR_SIZE,24

# General configuration
general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    gaps_in = 5
    gaps_out = 20
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)
    layout = master
}

# Output configuration
monitor = eDP-1,1920x1080@59.999001,0x0,1

# Input configuration
input {
    kb_layout = us
    kb_variant = intl
    follow_mouse = 0
    touchpad {
        natural_scroll = true
    }
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    numlock_by_default = true
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    rounding = 10
    blur {
        enabled = true
        size = 3
        passes = 1
    }
    drop_shadow = true
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = true
    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    mfact = 0.65
    new_on_top = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device:epic-mouse-v1 {
    sensitivity = -0.5
}

# Variables
# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Binds
## Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, alacritty
bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
bind = $mainMod SHIFT, F, togglefloating,
bind = $mainMod CTRL, F, fullscreen 
bind = $mainMod, F, layoutmsg, swapwithmaster master
bind = $mainMod, R, exec, tofi-drun --drun-launch=true
bind = $mainMod CTRL, R, exec, customized-bemenu

## Move focus with mainMod + arrow keys
bind = $mainMod, a, movefocus, l
bind = $mainMod, d, movefocus, r
bind = $mainMod, w, layoutmsg, cyclenext
bind = $mainMod, s, layoutmsg, cycleprev

## Move active window
bind = $mainMod SHIFT, a, movewindow, l
bind = $mainMod SHIFT, d, movewindow, r
bind = $mainMod SHIFT, w, layoutmsg, swapnext
bind = $mainMod SHIFT, s, layoutmsg, swapprev

## Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

## Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

## Resizing
bind = $mainMod CTRL, a, resizeactive, -10 0
bind = $mainMod CTRL, d, resizeactive, 10 0
bind = $mainMod CTRL, w, resizeactive, 0 10
bind = $mainMod CTRL, s, resizeactive, 0 -10

## Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

## Tabbed
bind = $mainMod SHIFT, TAB, togglegroup
bind = CTRL, TAB, changegroupactive, f
bind = CTRL SHIFT, TAB, changegroupactive, b
### Move window with mainMod + motion keys
bind = $mainMod SHIFT, left, movewindoworgroup, l
bind = $mainMod SHIFT, right, movewindoworgroup, r
bind = $mainMod SHIFT, up, movewindoworgroup, u
bind = $mainMod SHIFT, down, movewindoworgroup, d

## Screenshot
bind = $mainMod, SPACE, exec, grim -g "$(slurp)" - | swappy -f -

## Volume and Media Control
bind = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bind = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bind = , XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle
bind = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioPause, exec, playerctl play-pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous
bind = , XF86AudioStop, exec, playerctl stop

## Screen brightness
bind = , XF86MonBrightnessUp, exec, brightnessctl set +5%
bind = , XF86MonBrightnessDown, exec, brightnessctl set 5%-

## Special workspace
bind = $mainMod SHIFT, dead_grave, movetoworkspace, special
bind = $mainMod, dead_grave, togglespecialworkspace

# Window rules
# Example windowrule v1
# windowrule = float, ^(keepassxc)$
# Example windowrule v2
windowrulev2 = float,class:^(org.keepassxc.KeePassXC)$
windowrulev2 = float,class:^(thunar)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more